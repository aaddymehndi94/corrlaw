[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "corrlaw"
version = "0.0.0"
description = "Research scaffold for ambiguity-aware physical equation discovery"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
