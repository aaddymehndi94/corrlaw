# Pre-confirmation skeptical review — overnight extension

Written while revised A/B development002 runs, before new C/D/E/F PySR outcomes.
No scientific tuning is justified by desired performance or a favorable hypothesis.

- The implemented question concerns a dimensionless generic feature grammar. It is
  neither a blind coding-agent experiment nor raw-variable symbolic regression.
  True A–E terms occur because a generic low-degree rule enumerates all features,
  not because the learner imports the evaluator's answer. F enters the richer PySR
  rational composition class, although it is absent from the old finite linear span.
- The discovered10/31 complexity mismatch has been corrected. Every ordinary and
  added accepted expression must fit the same15-node arithmetic grammar as search.
  The expanded physical expression can be more complex because each feature terminal
  has unit cost. This choice is common to all policies and must remain explicit.
- Point selection is shared on identical measurements. Full-data candidate history
  is retained and revalidated; bootstrap-only candidates affect acquisition. Literal
  formula deduplication may retain algebraic duplicates, influencing equal-weight
  variance. No posterior interpretation is justified. The strengthened audit checks
  random streams and actual equality of point models on shared histories.
- D-optimal is the regularized linear-feature information criterion on the generic
  dictionary. It is a strong comparator for the geometric witness directions, but
  is NOT a solution to nonlinear optimal design for the entire rational PySR class.
  The known variance/leverage overlap concerns this stated feature-space criterion.
  Report the actual query agreements and outcomes; do not infer universal equality.
- Nominal search counts, populations, iteration and complexity caps are shared.
  Dropout changes the bootstrap search space and may change runtime. Witness
  instrumentation runs for all policies. Report measured witness cost separately;
  equal nominal search budgets are not a claim of identical wall-clock expense.
- Fitness reweights acquired rows so total query weight equals the128 original
  fitting rows. Candidate selection adds acquired MSE separately. Both choices
  are shared; differences from the finite pilot confound cross-engine comparisons.
- Finiteness on observed/calibration/public pool/probe points does not prove a
  rational function has no pole elsewhere. Hidden-test failures remain failures.
  SymPy simplification can fill removable singularities; the saved canonical formula
  defines numerical evaluation. No global physical-validity certificate is supplied.
- Geometry alone is insufficient. Both explicit equations must satisfy class/fit
  checks. The.002 floor permits approximate disagreement at zero injected noise;
  this is not a theorem of exact observational indistinguishability. Failure to
  construct a witness is not uniqueness. Inadmissible point models are separately
  flagged, including singleton fallbacks with misleadingly low ensemble spread.
- Every query is an available candidate ID, with a saved coordinate/label/noise ID
  and no repeats. Calibration labels are used, counted, and never called hidden.
  Hidden labels enter only evaluation. Broad unlabeled probes remain available for
  computation even under restricted measurement pools, as declared in the benchmark.
- Conditions/test points are not independent replicates. Aggregate within seed and
  expose all conditions. The strong tier must compare only its matched primary
  conditions. Never contrast its narrow aggregate with the wider primary mean.
- Additional priors can resolve ambiguity in a restricted class. Origin plus parity
  removes cubic circle aliases, while suitable quartic alternatives survive origin,
  parity and global positivity. Full dimensional information fixes A/C/D structures
  under no-additional-parameter assumptions. These are limits on physical claims.
- The saved-committee diagnostic asks whether adding alternatives raises a spread
  flag on known large errors while holding the predictor/history fixed. Its result
  is separate from query value. Adding central or duplicate candidates can reduce
  variance; record both directions. Threshold crossings do not calibrate confidence.
- Preserve historical finite v1/v2 and early PySR failures. They are not secretly
  excluded confirmation observations. Development001 was deliberately superseded
  before the new held-out experiment, and its interrupted record remains intact.
