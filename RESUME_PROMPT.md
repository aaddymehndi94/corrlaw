Resume CorrLaw in this repository using `AGENTS.md` and `CODEX_PROMPT.md`.
Read `.research/STATE.md`, `.research/HANDOFF.md`, and `.research/PLAN.md` first.
Inspect Git status, `python3 tools/lab.py status`, and the relevant existing run
records. Do not restart the sprint clock, recreate completed work, overwrite run
IDs, or assume the previous chat is available. Preserve all user edits and staged
changes. Reconcile interrupted runs only after checking whether their processes
are still active. Continue the first unfinished, unblocked task under the recorded
remaining budget, updating records and making safe local progress commits. No push,
publication, extra paid services, or recursive agent sessions. If the recorded
budget is exhausted, give a concise evidence-based handoff without starting new work.
